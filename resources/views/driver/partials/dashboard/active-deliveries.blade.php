<section class="overflow-hidden rounded-md border border-slate-200 bg-white shadow-sm">
    <div class="flex items-center justify-between border-b border-slate-100 px-5 py-4"><div><h2 class="font-extrabold text-[#071d4f]">Assigned and pending trips</h2><p class="mt-1 text-xs text-slate-500">Paid trips ready for delivery appear first.</p></div><a href="{{ route('driver.deliveries.index') }}" class="text-xs font-bold text-violet-700">View all trips</a></div>
    <div class="divide-y divide-slate-100">@forelse ($activeDeliveries as $delivery)
        <article class="flex flex-col justify-between gap-3 px-5 py-4 sm:flex-row sm:items-center"><div><div class="flex flex-wrap items-center gap-2"><p class="font-extrabold text-[#071d4f]">{{ $delivery->reference }}</p><span class="rounded px-2 py-1 text-[11px] font-bold {{ $delivery->payment_status === \App\Models\ShoppingList::PAYMENT_PAID ? 'bg-emerald-50 text-emerald-700' : 'bg-amber-50 text-amber-700' }}">{{ $delivery->payment_status === \App\Models\ShoppingList::PAYMENT_PAID ? 'Ready to deliver' : 'Pending payment' }}</span></div><p class="mt-1 text-sm font-semibold text-slate-700">{{ $delivery->school?->name ?? $delivery->school_name ?? 'Delivery location pending' }}</p><p class="mt-1 text-xs text-slate-500">{{ $delivery->parent_name }} | {{ $delivery->phone }}</p></div><a href="{{ route('driver.deliveries.index', ['q' => $delivery->reference]) }}" class="inline-flex min-h-10 items-center justify-center rounded-md border border-slate-200 px-4 text-sm font-bold text-[#071d4f] hover:bg-slate-50">View trip</a></article>
    @empty<p class="px-5 py-12 text-center text-sm text-slate-500">No pending trips. New assignments will appear here.</p>@endforelse</div>
</section>
